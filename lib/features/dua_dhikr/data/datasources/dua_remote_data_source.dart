// ─── Dua Remote Data Source ───────────────────────────────────────────────────
//
// Stub — Duas are fully bundled in assets/data/duas.json (offline-first).
//
// Extension point: if a remote Dua API is integrated in the future, add Dio
// calls here and wire them into DuaRepositoryImpl as a remote-first fallback.
//
// Candidate API: https://api.hisnmuslim.com (community-maintained)
//
class DuaRemoteDataSource {
  // No remote API is currently integrated.
}
