<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- ── Core Permissions ─────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Exact alarms: USE_EXACT_ALARM (API 33+, auto-granted) covers newer
         devices; SCHEDULE_EXACT_ALARM (API 31-32) requires user consent. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- POST_NOTIFICATIONS required on Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required to open ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS directly -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- ── App Blocker Permissions ──────────────────────────────────────────── -->
    <!-- Needed to start BlockerOverlayActivity from the Accessibility Service. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:label="Prayer Lock"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ── App Blocker ──────────────────────────────────────────────────── -->

        <!-- Accessibility Service: event-driven foreground app detection.
             Replaces the old UsageStatsManager polling foreground service.
             User must enable manually in system settings — never auto-enabled. -->
        <service
            android:name=".PrayerLockAccessibilityService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Receives AlarmManager broadcasts at prayer-window start/end and
             toggles the window_active flag the Accessibility Service reads. -->
        <receiver
            android:name=".BlockerWindowReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.mdnahid.prayerlock.BLOCK_WINDOW_START" />
                <action android:name="com.mdnahid.prayerlock.BLOCK_WINDOW_END" />
            </intent-filter>
        </receiver>

        <!-- Full-screen overlay shown when a blocked app is detected -->
        <activity
            android:name=".BlockerOverlayActivity"
            android:exported="false"
            android:theme="@style/BlockerOverlayTheme"
            android:launchMode="singleTop"
            android:taskAffinity="com.mdnahid.prayerlock.blocker"
            android:excludeFromRecents="true"
            android:showOnLockScreen="true"
            android:turnScreenOn="true" />

        <!-- ── android_alarm_manager_plus ────────────────────────────────────
             These components are normally merged automatically from the plugin's
             own manifest, but declaring them explicitly ensures they are present
             even if the merge tool strips them in a release build.          -->

        <!-- Background service that executes the Dart alarm callback -->
        <service
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />

        <!-- Receives the alarm intent and starts AlarmService -->
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmBroadcastReceiver"
            android:exported="false" />

        <!-- Re-schedules alarms after device reboot (rescheduleOnReboot: true) -->
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.RebootBroadcastReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- ── flutter_local_notifications ───────────────────────────────────
             Allows the plugin to receive notification tap actions.          -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- ── Native prayer alarm system ────────────────────────────────────
             PrayerAlarmReceiver: fired directly by AlarmManager — no Flutter
             Dart isolate involved, so it is immune to OEM battery-optimisation
             killing (Xiaomi MIUI, Infinix HiOS, OPPO ColorOS, etc.).
             exported="false": AlarmManager's PendingIntent is an explicit
             broadcast (Intent(context, PrayerAlarmReceiver::class)) so the
             receiver does not need to be exported.                           -->
        <receiver
            android:name=".PrayerAlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- PrayerBootReceiver: reschedules prayer alarms after reboot.
             Listens for BOOT_COMPLETED, QUICKBOOT_POWERON, and
             MY_PACKAGE_REPLACED (covers OTA app updates).                   -->
        <receiver
            android:name=".PrayerBootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- ── Home Screen Widget ───────────────────────────────────────────
             Shows the next prayer name, time, and countdown at a glance.
             Bound data is written by HomeWidgetService.dart via the
             home_widget plugin; PrayerWidgetProvider binds it to the layout
             and wires a launch-app tap intent.                               -->
        <receiver
            android:name=".PrayerWidgetProvider"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/prayer_widget_info" />
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
        <!-- Launcher intent: lets the App Blocker enumerate user-installed
             apps with a launcher activity, without QUERY_ALL_PACKAGES.
             This is the Play-Store-recommended package-visibility approach. -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>
</manifest>
