<?xml version="1.0" encoding="utf-8"?>
<!--
  Accessibility service config for Prayer Lock.

  Scope is intentionally minimal:
    - typeWindowStateChanged only — fires once when an app becomes foreground.
    - canRetrieveWindowContent="false" — we never read screen content; only
      the package name from the event itself. Keeps Play Store review simple.
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="false"
    android:notificationTimeout="100"
    android:description="@string/accessibility_service_description"
    android:summary="@string/accessibility_service_summary" />
