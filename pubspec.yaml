name: prayer_lock
description: "Prayer Lock - Daily spiritual companion app for Muslims."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.7.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8

  # State Management
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  # Location Services
  geolocator: ^13.0.2
  geocoding: ^4.0.0
  permission_handler: ^11.3.1

  # Database & Storage
  sqflite: ^2.4.1
  path: ^1.9.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  shared_preferences: ^2.3.3

  # Notifications & Alarms
  flutter_local_notifications: ^18.0.1
  android_alarm_manager_plus: ^4.0.5

  # Media
  audioplayers: ^6.1.0

  # Islamic Features
  flutter_qiblah: ^3.1.0+1
  hijri: ^3.0.0

  # Firebase & Auth
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.4
  firebase_crashlytics: ^4.3.0
  cloud_firestore: ^5.6.0
  google_sign_in: ^6.2.2

  # Monetization
  purchases_flutter: ^9.16.0

  # Networking
  dio: ^5.7.0
  http: ^1.2.2

  # Utilities
  logger: ^2.5.0
  dartz: ^0.10.1
  intl: ^0.19.0

  # UI Enhancements
  flutter_svg: ^2.0.16
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linting
  flutter_lints: ^5.0.0

  # Code Generation
  build_runner: ^2.4.14
  hive_generator: ^2.0.1
  riverpod_generator: ^2.6.2

  # App icon generation — run with: dart run flutter_launcher_icons
  flutter_launcher_icons: ^0.14.1

  # Testing
  mockito: ^5.4.4

# ─── App launcher icons ─────────────────────────────────────────────────────
# Regenerate after changing the source image:
#   dart run flutter_launcher_icons
flutter_launcher_icons:
  android: "ic_launcher"
  ios: true
  image_path: "assets/images/logo/prayer-lock-icon.png"
  min_sdk_android: 26
  remove_alpha_ios: true
  background_color_ios: "#E8E2CF"
  adaptive_icon_background: "#E8E2CF"
  adaptive_icon_foreground: "assets/images/logo/prayer-lock-icon.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Assets
  assets:
    - assets/images/
    - assets/images/icons/
    - assets/images/backgrounds/
    - assets/images/logo/
    - assets/audio/adhan/
    - assets/databases/
    - assets/data/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # Fonts
  # TODO: Add Arabic fonts (Amiri and TraditionalArabic)
  # Download Amiri from: https://fonts.google.com/specimen/Amiri
  # fonts:
  #   - family: Amiri
  #     fonts:
  #       - asset: assets/fonts/arabic/Amiri-Regular.ttf
  #       - asset: assets/fonts/arabic/Amiri-Bold.ttf
  #         weight: 700
  #   - family: TraditionalArabic
  #     fonts:
  #       - asset: assets/fonts/arabic/TraditionalArabic-Regular.ttf
  #       - asset: assets/fonts/arabic/TraditionalArabic-Bold.ttf
  #         weight: 700
